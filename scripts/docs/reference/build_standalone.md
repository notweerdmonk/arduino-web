# build_standalone.sh

Build standalone binaries via PyOxidizer.

## Overview

Build all or selected apps via PyOxidizer and package
into .tar.gz or .zip archives with a smoke test for each binary.


