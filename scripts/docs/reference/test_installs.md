# test_installs.sh

Install monorepo wheels and run smoke tests.

## Overview

Create a clean pipenv venv, sync all 6 monorepo wheels
from Pipfile, and run import + --help smoke tests on each package.


