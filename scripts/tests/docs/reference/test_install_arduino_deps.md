# test_install_arduino_deps.sh

Tests for the install_arduino_deps.sh script.

## Overview

Verify file checks, bash syntax, missing arduino-cli
handling, and successful execution with a fake arduino-cli shim.


